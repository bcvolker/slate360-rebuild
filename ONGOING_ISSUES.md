# Slate360 Ongoing Issues

Last updated: 2026-04-17

This is the durable root issue tracker for the active Phase 1 repair program. It is intended to preserve the current business-logic, routing, and product-surface concerns while implementation proceeds in smaller slices.

Status values:

- `open`
- `investigating`
- `fixing`
- `testing`
- `done`

Fix timing values:

- `before design`
- `during design`
- `after Phase 1 beta`

## Seeded Issues

| Issue ID | Title | Severity | Beta blocker? | Area | Current factual evidence | Exact files/routes/components involved | Proposed fix direction | Fix timing | Blast radius | Status | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| S360-001 | Auth logo dark-mode background defect | Medium | No | Auth / onboarding / install | Fixed: new SVG assets without `prefers-color-scheme: dark` rule, all 26 logo paths updated repo-wide. | `public/uploads/slate360-logo-reversed-v2.svg`; all auth pages | Fixed in PR #3 (branding-assets hotfix), merged 2026-04-17. | before design | Low | done | Fixed — new dark-mode-safe SVGs deployed to production. |
| S360-002 | Auth input and label text are undersized on mobile | High | Yes | Auth / onboarding / install | Shared auth classes use `text-sm` for inputs and `text-xs` for labels/help text, which is too small for comfortable mobile auth and can trigger iOS zoom. | `app/globals.css`; `app/login/page.tsx`; `app/signup/page.tsx`; `app/forgot-password/page.tsx` | Increase auth input/label/help text tokens and verify keyboard-safe mobile auth behavior. | before design | Low | open | Pre-beta usability issue, not just visual polish. |
| S360-003 | Confirm-email guidance is incomplete | Medium | No | Auth / onboarding / install | Signup confirmation exists, but the post-signup messaging does not clearly explain that login remains incomplete until email confirmation. | `components/auth/SignupConfirmation.tsx`; `app/api/auth/signup/route.ts`; `app/signup/page.tsx` | Clarify confirmation-state messaging and next-step language. | before design | Low | open | Can be fixed without redesigning auth. |
| S360-004 | First-run onboarding route is missing | Critical | Yes | Auth / onboarding / install | After successful confirm/callback, users default to `/dashboard`; no dedicated onboarding route or first-run sequence was found. | `app/auth/confirm/route.ts`; `app/auth/callback/route.ts`; `/dashboard` | Add a dedicated first-run path that connects auth completion, install guidance, and initial product orientation. | before design | Medium | open | Business-logic gap, not just UX copy. |
| S360-005 | Install center is only a narrow PWA instructions page | High | Yes | Auth / onboarding / install | `/install` exists, but only provides install instructions and installed-state detection. It is not an update center or permissions center. | `app/install/page.tsx`; `app/install/InstallClient.tsx`; `app/manifest.ts` | Keep the route, but re-scope its product promise and later expand it into a real install/download/update surface. | before design | Medium | open | Current copy and expected behavior must stay truthful. |
| S360-006 | Permissions explanation gap for app-first workflows | Medium | No | Auth / onboarding / install | No route or install-adjacent UI explains camera, photos, location, notification, or storage permissions even though Site Walk relies on them. | `app/install/InstallClient.tsx`; Site Walk capture surfaces | Add a permissions-readiness section tied to install/onboarding rather than hiding this until failure states. | during design | Medium | open | Important for mobile trust but not the first blocker once core onboarding exists. |
| S360-007 | Product surface contract remains confusing across homepage, web command center, and installed app | Critical | Yes | Product surfaces | `docs/specs/SHELL_CONTRACT.md` defines three surfaces, but the installed shell still starts at `/dashboard` and live navigation/layout emphasis still resembles the desktop command center. | `docs/specs/SHELL_CONTRACT.md`; `app/manifest.ts`; `/dashboard`; shared nav components | Keep one business-logic system, but explicitly reshape routing and surface language around the three-surface contract. | before design | High | open | This is the top-level alignment issue driving several downstream bugs. |
| S360-008 | Installed app still behaves too much like the web command center | High | Yes | Product surfaces | The installed PWA start route remains `/dashboard`, and no installed-shell-specific entry emphasis was found. | `app/manifest.ts`; `/dashboard`; install flow | Add installed-shell entry treatment without forking product logic. | before design | High | open | Separate from broad redesign because it affects launch contract. |
| S360-009 | Command center has not yet been reworked into the web version of the app | High | Yes | Command Center | The active owner path now consistently says `Command Center` instead of `Dashboard`, but the route still behaves as a thin summary shell rather than the intended actionable web app surface. | `components/walled-garden-dashboard.tsx`; `components/dashboard/command-center/CommandCenterContent.tsx`; `components/dashboard/command-center/DashboardTopBar.tsx`; `SLATE360_MASTER_BUILD_PLAN.md` | Rebuild the command center around live status, drillable actions, and real work queues, not decorative summary blocks. | before design | Medium | open | Visible naming mismatch fixed in the active logged-in shell; structural command-center rebuild still remains. |
| S360-010 | Quick Actions block is oversized relative to its value | Medium | No | Command Center | Quick Actions consumes a prominent grid slot with four large buttons, while more important operational context is missing. | `components/dashboard/command-center/QuickActionsCard.tsx`; `components/dashboard/command-center/CommandCenterContent.tsx` | Shrink or rebalance quick actions after the real command-center content hierarchy is decided. | during design | Medium | open | Should not be fixed in isolation from command-center hierarchy. |
| S360-011 | Dashboard duplicates project emphasis and uses low-value counters | Medium | No | Command Center / Projects | Dashboard quick actions include both `New Project` and `Open Projects`, and the project overview card uses simple counts for active/completed/on-hold without deeper operational value. | `components/dashboard/command-center/QuickActionsCard.tsx`; `components/dashboard/command-center/ProjectOverviewCard.tsx` | Replace duplicate project affordances and low-value counters with more actionable project status signals. | during design | Medium | open | Tied to the command-center rebuild rather than a one-off card tweak. |
| S360-012 | Recent Files and storage blocks are too weak for Phase 1 trust | High | No | Command Center / data visibility | Recent files only link to project SlateDrop roots, and the storage card exposes only one usage metric without credit or data context. | `components/dashboard/command-center/RecentFilesCard.tsx`; `components/dashboard/command-center/StorageCreditsCard.tsx`; `app/api/dashboard/summary/route.ts` | Deepen recent-file drilldown and unify usage/data presentation once canonical truth sources are defined. | before design | Medium | open | This is partly a UI problem and partly a source-of-truth problem. |
| S360-013 | SlateDrop root model is mixed and Phase 1 IA is still wrong | Critical | Yes | SlateDrop | The active project-scoped SlateDrop route now presents a clearer `Project Files` shell with project-aware breadcrumb labeling and embedded list-first behavior, but the broader root-model decision and base folder architecture are still not fully settled. | `lib/slatedrop/folderTree.ts`; `app/(dashboard)/projects/[projectId]/slatedrop/page.tsx`; `lib/slatedrop/provisioning.ts`; `components/slatedrop/SlateDropClient.tsx` | Keep the active file experience project-scoped, then finish the final Phase 1 file IA and provisioning contract. | before design | High | testing | Project-scoped visible packaging improved in this slice; the global IA decision is still open. |
| S360-014 | Non-canonical `/slatedrop` assumptions still leak through nav and product language | High | Yes | SlateDrop / navigation | Shared and mobile app-shell navigation no longer advertises `/slatedrop` as a global root destination, but remaining non-shell references still need verification and cleanup. | `app/slatedrop/page.tsx`; `components/shared/QuickNav.tsx`; `components/shared/MobileNavSheet.tsx`; `components/shared/MobileModuleBar.tsx`; `components/dashboard/MobileQuickAccess.tsx`; `components/home/landing-data.ts` | Keep `/slatedrop` out of active app-shell nav, and clean remaining non-canonical references in later slices. | before design | Medium | testing | Root app-shell exposure was removed in this slice. |
| S360-015 | Mobile File Explorer / Finder expectations do not match current SlateDrop UX | Medium | No | SlateDrop / mobile | The project-scoped file surface now defaults to list mode when embedded, the toolbar wraps on smaller screens, and folder/file grids collapse to one column on mobile, but the experience still has broader app-shell and IA debt beyond this visible correction pass. | `components/slatedrop/SlateDropClient.tsx`; `components/slatedrop/ProjectFileExplorer.tsx`; `components/slatedrop/SlateDropToolbar.tsx`; `components/slatedrop/SlateDropFileArea.tsx`; project-scoped SlateDrop route | Treat this as part of the SlateDrop rebuild, not a piecemeal polish pass. | during design | High | testing | Mobile usability improved enough for owner-path verification, but the long-term rebuild decision still stands. |
| S360-016 | Legacy project contamination risk remains in the repo contract | High | No | Projects | The active `/projects` owner path no longer uses the old portfolio/workspace composition, but deeper `/projects/[projectId]/*` wrappers still re-export legacy `project-hub` routes and preserve contamination risk. | `SLATE360_MASTER_BUILD_PLAN.md`; `docs/specs/SHELL_CONTRACT.md`; project-related routes and components | Keep legacy trees in place for now, but continue forcing all active Phase 1 work through `/projects` and project-scoped module routes. | before design | Medium | testing | Active landing surface corrected in this slice; deeper wrapper-backed routes still need a later cleanup decision. |
| S360-017 | Project creation is tightly coupled to folder provisioning | Medium | No | Projects / SlateDrop | Project creation automatically provisions file structure, which is useful, but the coupling raises risk while the final SlateDrop IA is still unsettled. | `lib/slatedrop/provisioning.ts`; project creation flow; `/api/projects/create` | Preserve the coupling short term, but revisit the exact provisioning contract when the Phase 1 folder model is finalized. | during design | High | open | Avoid schema churn before the folder model decision is made. |
| S360-018 | Project routing, versioning, and history expectations are not yet explicit | Medium | No | Projects | The active `/projects` surface is real, but version/history expectations around project records and file history are not yet clearly represented in the Phase 1 plan. | `/projects`; `/projects/[projectId]`; `lib/slatedrop/folderTree.ts`; `ONGOING_ISSUES.md` | Add explicit project-routing and history assumptions to the repair plan before expanding file/version UX. | during design | Medium | open | Planning issue more than a bug. |
| S360-019 | Site Walk still has verified Phase 1 capability gaps | Critical | Yes | Site Walk | Core capture/review/share/export flows are real, but project selection is partly orphaned, unattached start is missing, and several Phase 1 promises are still partial. | `app/site-walk/[projectId]/sessions/*`; `components/site-walk/ProjectSelectorClient.tsx`; `app/api/site-walk/sessions/route.ts` | Close the remaining Phase 1 workflow holes before broad visual work. | before design | Medium | open | Site Walk is real enough to harden, not restart. |
| S360-020 | Site Walk deliverable persistence remains uncertain | Critical | Yes | Site Walk | The deliverable editor exposes editing and save affordances, but this pass did not verify a complete persistence path for edited content. | `components/site-walk/BlockEditor.tsx`; `app/site-walk/[projectId]/deliverables/new/page.tsx`; `app/api/site-walk/deliverables/route.ts`; `app/api/site-walk/deliverables/[id]/route.ts` | Verify and finish the save path end-to-end before promising editable deliverables. | before design | High | investigating | Treat as a trust bug until save semantics are proven. |
| S360-021 | Site Walk plan image route was missing | Critical | Yes | Site Walk | Fixed in code: added `/api/site-walk/plans/[id]/image`, which verifies org scope and redirects to a signed inline S3 image URL for the stored plan asset. | `components/site-walk/PlanViewer.tsx`; `app/api/site-walk/plans/[id]/image/route.ts`; `app/api/site-walk/plans/[id]/route.ts` | Keep plan image delivery on the stored `site_walk_plans.s3_key` contract and verify existing plans load in the viewer. | before design | Medium | testing | This closes the concrete missing-route blocker without changing the viewer contract. |
| S360-033 | Site Walk photo uploads bypassed the project SlateDrop system | Critical | Yes | Site Walk / SlateDrop | Fixed in code: `/api/site-walk/upload` now reserves a pending `slatedrop_uploads` row in the owning project's `Photos` folder, and `/api/site-walk/items` now activates that reserved file instead of leaving captures in a disconnected `site-walk/photos/` silo. | `app/api/site-walk/upload/route.ts`; `app/api/site-walk/items/route.ts`; `lib/slatedrop/storage.ts`; `project_folders`; `slatedrop_uploads` | Keep Site Walk capture uploads on the canonical SlateDrop path and use the legacy bridge only as a fallback for older callers or already-siloed keys. | before design | High | testing | This closes the specific Beta launch doctrine gap for new captures; older legacy keys may still need one-time migration if they must appear in project folders retroactively. |
| S360-034 | Project helper APIs assumed org-wide membership instead of project scope | Critical | Yes | Projects / collaborators | Fixed in code: recent files, saved records, photo reports, management contracts, and management reports now derive effective org scope from the resolved project instead of requiring a separate `organization_members` lookup to succeed. | `app/api/projects/[projectId]/recent-files/route.ts`; `app/api/projects/[projectId]/records/route.ts`; `app/api/projects/[projectId]/photo-report/route.ts`; `app/api/projects/[projectId]/management/contracts/route.ts`; `app/api/projects/[projectId]/management/reports/route.ts` | Keep project-scoped APIs anchored to resolved project access so collaborator invites do not fail on secondary storage/report operations. | before design | High | testing | This is the beta hardening pass for collaborator access on the helper routes most likely to break after invite redemption. |
| S360-035 | Cloudflare R2 direct browser uploads were blocked by bucket CORS | High | Yes | SlateDrop / storage | Fixed in runtime configuration: after the app CSP was updated to allow `*.r2.cloudflarestorage.com`, the missing Cloudflare bucket CORS rule was added and the public upload/share smoke test advanced past the presigned PUT step. | `next.config.ts`; `middleware.ts`; `app/api/slatedrop/upload-url/route.ts`; `app/upload/[token]/upload-portal-client.tsx`; `scripts/ops/smoke-slatedrop-public-flow.mjs` | Keep the Cloudflare bucket CORS rule in place for local, Codespaces, and production app origins. | before design | High | testing | `npm run smoke:slatedrop-public-flow` now passes the browser upload step and the full public flow. |
| S360-036 | SlateDrop share links drifted from live unified_files foreign key | Critical | Yes | SlateDrop / sharing | Fixed in code and schema tracking: the live database already expected `slate_drop_links.file_id -> unified_files(id)`, while the repo still created share links from `slatedrop_uploads.id`. Upload completion and secure-send now bridge uploads into `unified_files`, and public share pages read `unified_files` first with a legacy fallback. | `app/api/slatedrop/complete/route.ts`; `app/api/slatedrop/secure-send/route.ts`; `app/share/[token]/page.tsx`; `lib/slatedrop/unified-files.ts`; `supabase/migrations/20260418101500_track_unified_files_and_slatedrop_bridge.sql`; `scripts/ops/smoke-slatedrop-public-flow.mjs` | Keep the `slatedrop_uploads.unified_file_id` bridge populated on completion and preserve the tracked `unified_files` migration so future environments do not drift again. | before design | High | testing | Verified by a full green `npm run smoke:slatedrop-public-flow` run against the local app and live Supabase. |
| S360-037 | Public-facing shell still looked like a different product than the app | High | Yes | Homepage / public surfaces | Partially fixed in code and promoted to `main`: the old live homepage monolith no longer drives `/`, which now renders the extracted `components/home/LandingPage.tsx` stack with shared design-system primitives in its hero, product, pricing, and CTA sections; shared `SlateLogo` usage now covers the visible homepage, auth, dashboard, Site Walk, SlateDrop, and external-response shells. | `app/page.tsx`; `components/home/LandingPage.tsx`; `components/home/LandingHeader.tsx`; `components/home/HeroSection.tsx`; `components/home/AppShowcaseSection.tsx`; `components/home/PricingSection.tsx`; `components/home/CTASection.tsx`; `components/shared/SlateLogo.tsx` | Keep the extracted `components/home/*` stack as the only live homepage system, then continue aligning section anatomy and dashboard shell primitives instead of returning to `components/marketing-homepage.tsx`. | before design | Medium | testing | Commit `7cce7b9` promoted this cutover to `main`; Vercel production deploy for that commit is now building. |
| S360-038 | PWA home-screen icon used stale generic S mark | Critical | Yes | PWA / branding | Fixed in code: the manifest and app metadata now prioritize the correct PNG PWA icons, and all active favicon/icon SVG fallbacks were replaced with the updated geometric Slate360 mark on a dark background. | `app/manifest.ts`; `app/layout.tsx`; `app/icon.svg`; `public/icon-v2.svg`; `public/uploads/slate360-favicon-v2.svg`; `public/uploads/favicon.svg`; `public/uploads/icon-192.png`; `public/uploads/icon-512.png`; `public/uploads/icon-512-maskable.png` | Keep PWA manifest icons, apple icons, favicon SVGs, and app route icon assets aligned whenever the brand mark changes. | before design | Low | testing | Existing installed home-screen icons may remain cached by iOS/Android; remove and re-add the PWA after deployment to verify the new install icon. |
| S360-039 | Mobile refresh rendered unstyled text-only page from stale service-worker caches | Critical | Yes | PWA / service worker | Fixed in code: the service worker no longer precaches or runtime-caches Next HTML/CSS/JS, activation deletes all Cache Storage entries, and the client registrar forces one reload when the new controller takes over. | `app/sw.ts`; `components/providers/SWRegistrar.tsx`; `app/layout.tsx`; generated `/sw.js` | Keep Next documents and `_next/static` assets network/browser-cache controlled; do not reintroduce service-worker HTML/CSS/JS caching without a versioned rollout and mobile refresh test. | before design | High | testing | Root cause matches the phone symptom: stale cached HTML can point at retired CSS chunks after deploy, leaving a mostly white text-only page. |
| S360-022 | Site Walk viewer/share workflow still has collaboration and response gaps | High | No | Site Walk | Public share links and PDF export exist, but the viewer is mostly static and this pass did not confirm item-level responses, autosave guarantees, AI formatting, or collaboration flows. | `app/share/deliverable/[token]/page.tsx`; `components/site-walk/DeliverableViewer.tsx`; share/export APIs | Verify the share/viewer contract and explicitly defer non-beta collaboration features if they are not shipping in Phase 1. | during design | Medium | open | Split “must verify” from “can defer” before adding more viewer complexity. |
| S360-023 | Operations Console scope is misnamed and too narrow | High | No | Operations Console | Partially fixed: Operations Console now has count-badged cards for pending Version 1 access, open feedback, and feature/function suggestions, plus scaffolded business-control workflows for access extensions, app grants, pricing, trials, enterprise seats, product health, and systems. Backend mutations for most controls remain pending. | `app/(dashboard)/operations-console/page.tsx`; `components/dashboard/OperationsConsoleClient.tsx`; `components/dashboard/operations-console/OperationsConsoleNav.tsx`; `app/(dashboard)/operations-console/[section]/page.tsx`; `lib/server/operations-console-counts.ts`; `app/api/admin/beta/route.ts` | Continue expanding with audited mutations for trial extension, app access grants, pricing/plan edits, and enterprise seat management. | before design | Medium | testing | Name now has a roadmap-backed UI; do not mark done until backend control APIs exist. |
| S360-024 | Runtime/test account pollution is unresolved | High | No | Operations Console / platform ops | Suspicious runtime and debug accounts are visible operationally, but no direct generator path was found in repo code during this pass. | `components/dashboard/OperationsConsoleClient.tsx`; `scripts/account-path-diagnostic.mjs`; `scripts/delete-test-users.mjs` | Investigate auth-provider logs, admin activity, and script execution history before making destructive cleanup decisions. | before design | Medium | investigating | Do not delete rows blindly. |
| S360-025 | Owner-specific behavior is leaking into normal subscriber narratives | High | No | Operations Console / billing / account | The My Account truth layer no longer derives fake credit usage from owner-sensitive balances, but Operations Console and owner-only access behavior still need broader cleanup. | `lib/server/beta-access.ts`; `components/dashboard/my-account/AccountBillingTab.tsx`; `components/dashboard/my-account/AccountDataTrackerTab.tsx`; `components/dashboard/OperationsConsoleClient.tsx` | Keep subscriber-visible account truth grounded in shared data, and defer broader owner/ops cleanup to later slices. | before design | Medium | testing | Narrowed in this slice to the active My Account truth layer. |
| S360-026 | Shared and mobile navigation expose the wrong Phase 1 routes | Critical | Yes | Navigation / mobile | Shared nav and mobile quick-access no longer advertise the global SlateDrop root, and the mobile sheet now applies the same Site Walk gating logic as desktop nav. | `components/shared/QuickNav.tsx`; `components/shared/MobileNavSheet.tsx`; `components/shared/MobileModuleBar.tsx`; `components/dashboard/MobileQuickAccess.tsx`; `docs/specs/SHELL_CONTRACT.md` | Verify the updated nav set in manual testing, then continue pruning remaining non-canonical references outside the shared shell. | before design | Medium | testing | Active shell navigation is aligned to safer Phase 1 destinations in this slice. |
| S360-027 | Site Walk shell loses logo consistency and broader app context | Medium | No | Navigation / mobile / shell | Site Walk workflow pages use a narrow header with back navigation and actions, but no shared logo or broader shell continuity. | `components/site-walk/SiteWalkNav.tsx`; Site Walk workflow routes | Revisit header consistency once the shell contract is locked, without forcing desktop chrome into mobile flows. | during design | Medium | open | Separate branding consistency from app-speed requirements. |
| S360-028 | My Account exposes a blank customize path | Medium | No | Navigation / mobile / account shell | My Account no longer passes through the shared customize affordance, and the visible owner-path customize remnants were also removed from active Photos and Punch List surfaces. | `components/shared/DashboardTabShell.tsx`; `components/dashboard/MyAccountShell.tsx`; `components/widgets/WidgetCustomizeDrawer.tsx`; `app/(dashboard)/projects/[projectId]/photos/page.tsx`; `app/(dashboard)/projects/[projectId]/punch-list/page.tsx` | Keep customize available only where it is meaningful. | before design | Low | done | Active blank/low-value customize affordances are removed from the owner verification path. |
| S360-029 | Horizontal overflow patterns are intentional but not yet mobile-validated | Low | No | Navigation / mobile | Several project/account tab shells rely on `overflow-x-auto`; this is not broken by itself, but the current mobile contract still needs validation. | project/account tab shells; mobile nav surfaces | Validate mobile interaction first, then redesign only if the actual flows remain cramped. | during design | Low | open | Not a blocker by itself. |
| S360-030 | Storage, file counts, and credits have no single canonical truth | Critical | Yes | Data / usage / credits | Dashboard summary and My Account now both derive storage and file counts from active `slatedrop_uploads`, and My Account no longer mixes storage truth from `organizations.org_storage_used_bytes` or file truth from `slatedrop_files`. | `app/api/dashboard/summary/route.ts`; `app/api/account/overview/route.ts`; `lib/server/usage-truth.ts`; `lib/entitlements.ts`; `lib/server/org-feature-flags.ts` | Verify the updated contract across dashboard, My Account, and project-scoped SlateDrop, then decide whether a dedicated credits ledger truth layer is still needed. | before design | High | testing | This slice established one shared upload-based storage/file-count source. |
| S360-031 | Owner override handling distorts account trust | High | No | Data / usage / credits | The active My Account route now avoids misleading credit-used math and preserves the same underlying balances for owner and subscriber views, but broader owner-only surfaces still need audit. | `components/dashboard/my-account/AccountBillingTab.tsx`; `components/dashboard/my-account/AccountDataTrackerTab.tsx` | Keep owner notes informational only and continue separating owner diagnostics from subscriber-facing truths. | before design | Medium | testing | Improved in this slice, not fully closed platform-wide. |
| S360-032 | Platform usage visibility gap across AWS, Supabase, and app usage | High | No | Data / usage / credits | The current UI does not expose a cohesive platform usage picture spanning storage, uploads, credits, entitlements, or backend usage health. | Dashboard and account usage surfaces; storage/credits APIs | Build a truthful Phase 1 usage layer after the canonical metric contract is settled. | during design | High | open | Do not add more cards before the source model is unified. |

## Notes

- This tracker intentionally preserves uncertain items as uncertain when repo evidence is incomplete.
- Do not mark issues `done` until the route/component/data-path evidence has been re-verified after the fix.
- If a future prompt changes product-surface assumptions, update this file and the active build-plan report in the same slice.