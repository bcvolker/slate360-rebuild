# Installed into the Modal GPU image at build time (see worker.py).
# Pin versions for reproducible deploys; bump intentionally after validation.
boto3==1.35.99
requests==2.32.3
numpy==2.2.1
nerfstudio==1.1.5
torch==2.5.1
torchvision==0.20.1
