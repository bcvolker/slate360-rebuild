# Splat Lab Python requirements.
# Install with: pip install -r workers/local/splat-lab/requirements.txt
# Slice 1 only needs ffmpeg on PATH; the rest are for later slices.

# Core (Slice 2 — real SfM + training)
nerfstudio>=0.3.0
gsplat>=1.0.0
onnxruntime-gpu>=1.18.0

# 360 equirect -> cubeface split (Slice 2)
numpy>=1.24
opencv-python>=4.8
py360convert>=0.1.0

# Utilities
pillow>=10.0
tqdm>=4.66
