# Chat sync — 2026-09-15

One place for every concurrent session (Claude desktop, Claude laptop, Cursor, homepage chat) to
read and append. Add a dated line under your track when you ship or decide something; don't paste
chat transcripts between sessions.

## Tracks

### Dashboard + client-account home (this doc's author)
- Worktree `C:\s360-dashboard-portal`, branch `feature/dashboard-portal-alignment-2026-09`,
  PR #34 → base `feature/aob205-spatial-experience-v3`. Not merged (awaiting Brian).
- Shipped: dashboard home reskinned to light `--mkt-*` tokens and wired to the real `/dashboard`
  route; new `/client-home` hero-carousel landing for spatial-only client logins; `/projects`
  redirects those clients there. Docs: `docs/design/DASHBOARD_PORTAL_AUDIT_2026-09.md`,
  `DASHBOARD_PORTAL_DESIGN_ALIGNMENT.md` (confirmed), `CLIENT_ACCOUNT_HOME_2026-09.md`.
- Verified desktop only; mobile shell not yet verified for `/client-home` (preview harnesses
  hardcode the desktop sidebar).
- Next candidates (Brian to pick): surface Tour/plan-pin deliverables inside the client project
  view; folder customization + templates (needs additive migration); CEO tools dashboard rethink.

### Homepage
- Worktree `C:\slate360-homepage-rebuild`, merges to `main`. Handoff:
  `docs/design/HOMEPAGE_STATUS_HANDOFF_2026-09-15.md`.
- 2026-09-15: stale pre-pivot `/product/digital-twin` page (drone / tripod-LiDAR / "survey-grade"
  claims) replaced with a redirect — PR #33, merged. Rule going forward: no laser-scanning claims
  (iPhone LiDAR only today); thermal stays a single low-key sentence.

### Spatial walkthrough concept
- `docs/design/SPATIAL_WALKTHROUGH_EXPLAINED.md` on `feature/aob205-spatial-experience-v3`
  (b962a837). Source of truth for what a walkthrough is and how the operator is removed.

### AOB205 portal UX polish
- `C:\s360-ux`, branch `feature/aob205-ux-polish-v3`, governed by
  `docs/ops/SPATIAL_EXPERIENCE_V3_UX_HANDOFF.md`. Owns `app/(public)/portal/[token]/**`,
  `components/external-portal/**`, `lib/spatial-experience/**`. Known open items from the audit
  doc above: `capabilities.ts` gating is dead code vs. the real loader; Plan page raster branch is
  unconditional; Documents lacks revision/date + empty state; Items pages lack the ask form.

### Desktop local processing (outside this repo)
- Brian's desktop app: drag-and-drop camera/drone/360 media → Gaussian splats + photogrammetry,
  local, "works like AirVis", can crop people out. Speed is the pain point. Output formats not yet
  documented here — record them when shared.

## Cross-track facts everyone should assume
- Business model: Brian captures and produces; contractors get a portal and reshare downstream.
  Contractor self-capture is future and must not appear on the homepage.
- Clients must never see Twin 360 / Site Walk naming. `lib/spatial-walkthrough/client-surface.ts`
  already enforces this for spatial-only orgs — verify new orgs get that flag.
- Mobile/tablet is a hard requirement for every client-facing surface.
- Already built, don't rebuild: per-project `01_Project_Info/Drawings` folder; PDF plan upload +
  rasterize pipeline; 360 Tour Builder with plan pins and a public mobile-first viewer
  (`/tours/view/[slug]`, builder CEO-only by design).
