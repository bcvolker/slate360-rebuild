Quick status update:

Pipeline: Core works and is validated on a real capture — metric geometry, floor plan, area
take-off, walk navigation, 201 tests passing, deployed. 360 video is not yet fused; alignment
is the open problem.

Website: No change. Still selling the old app subscriptions ($787–$3,476/yr, free trials,
app-store badges) rather than the services business.

Operator Dashboard: Not started. Separate studio routes exist but there is no single console
to run a job end to end — intake, processing, QC, assembly, publish.

Client Portal: Partial and fragmented. Pins, share tokens, tours and thermal sharing exist in
the database, but as three separate share systems rather than one project with a timeline.

Other: Services have not started — no paid captures, no Line 4 jobs. Kitchen showcase twin is
processed and viewable but not client-ready (14.2% untextured, no crop tooling). ASU
paperwork not started and on hold pending a conflict-of-interest check.

Biggest current blocker: There is no operator dashboard, so a job cannot be run end to end
and handed to a client.

Recommended next action (my view): Stop improving the pipeline and build operator dashboard
v1 — projects, intake, processing status, QC, publish.
