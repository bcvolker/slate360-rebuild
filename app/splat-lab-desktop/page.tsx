import { SplatLabPanel } from "@/components/splat-lab/SplatLabPanel";

export const metadata = {
  title: "Slate360 Splat",
};

export default function SplatLabDesktopPage() {
  return <SplatLabPanel clone="proven" />;
}
