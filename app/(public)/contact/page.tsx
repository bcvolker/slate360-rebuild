import { ContactPageShell } from "@/components/contact/ContactForm";

export const metadata = {
  title: "Contact — Slate360",
  description: "Contact the Slate360 team for access requests, enterprise pricing, and product questions.",
};

export default function ContactPage() {
  return <ContactPageShell />;
}
