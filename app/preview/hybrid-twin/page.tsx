import TwinMeshPreviewPage from "@/app/preview/twin-mesh/page";

/**
 * Hybrid twin V0.1 harness. Same kitchen walkthrough assets as /preview/twin-mesh
 * (Matterport navigation + metric mesh). The MeshTwinViewer chrome now includes
 * Reality/Hybrid/Geometry, capture-date selector, metric measure, and pins.
 */
export default TwinMeshPreviewPage;
