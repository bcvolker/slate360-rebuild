// The real Navbar implementation lives in NavbarFixed for now.
// This file re-exports it so other imports use `components/Navbar`.
import NavbarFixed from "./NavbarFixed";

export default NavbarFixed;