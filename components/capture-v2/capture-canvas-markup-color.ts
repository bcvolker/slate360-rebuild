import { fieldVisibility } from "@/lib/design-system/tokens";

/** Markup stroke color — mirrors --graphite-primary for vector tools. */
export const CAPTURE_CANVAS_MARKUP_COLOR = fieldVisibility.siteWalkGreen;
